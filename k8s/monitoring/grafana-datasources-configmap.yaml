apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: corebank
data:
  datasources.yaml: |
    apiVersion: 1
    datasources:
    - name: Prometheus
      type: prometheus
      uid: prometheus
      access: proxy
      url: http://prometheus:9090
      isDefault: true
      editable: true
